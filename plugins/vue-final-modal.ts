export default defineNuxtPlugin((nuxtApp) => {
  // Vue Final Modal v4 —可以直接 использовать как компонент,
  // но глобальная регистрация ensures работа в SSR
  // В Nuxt 4 компонент уже импортируется напрямую, поэтому плагин не обязателен.
})